<h1>Editing Book</h1>
<!-- form_for部分 -->
<%= form_for(@blog) do |f| %>
    <h4>title</h4>
    <%= f.text_field :title %>
    <h4>Body</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Update Book' %>
    <% end %>

<a class="Show"><%= link_to "show", book_path(book.id) %></a>
"|"
<a class="Back" href="/books">Back</a>